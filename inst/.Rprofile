if (interactive()) {source("renv/activate.R")}

library(envsetup)

# do not change the name of this object
# envsetup_config is a requirement for library to know how to reset autos
envsetup_config <- config::get(file = getOption("envsetup.config.path"))

rprofile(envsetup_config)
